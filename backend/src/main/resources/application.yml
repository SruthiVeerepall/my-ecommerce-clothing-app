server:
  port: 8080
  tomcat:
    max-http-form-post-size: 52428800

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ecommerce_db?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC&maxAllowedPacket=67108864
    username: root
    password: Sruthi@1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        jdbc:
          lob:
            non_contextual_creation: true
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME:sruthiveerepalli08@gmail.com}
    password: ${MAIL_PASSWORD:pdab xrsy ghup qgsj}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
        transport:
          protocol: smtp

# Verification code lifetime in minutes
app:
  verification:
    code-expiry-minutes: 10
    from-address: ${MAIL_USERNAME:sruthiveerepalli08@gmail.com}

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicTypeRegistry: TRACE

# Security settings
security:
  user:
    name: user
    password: password

# CORS configuration
cors:
  allowed-origins: "*"
  allowed-methods: "GET, POST, PUT, DELETE, OPTIONS"